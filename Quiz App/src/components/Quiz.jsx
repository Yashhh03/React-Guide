import { useState } from "react";


export default function Quiz() {
    cont [activeQuestionIndex, setActiveQuestionIndex] = useState(0);

    const [userAnswers , setUserAnswers] = useState([]);

    return(
        <p>Currently active question</p>
    );
}